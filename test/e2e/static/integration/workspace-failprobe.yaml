apiVersion: workspace.jupyter.org/v1alpha1
kind: Workspace
metadata:
  name: workspace-failprobe
  namespace: default
spec:
  desiredStatus: Running
  image: jk8s-application-jupyter-uv:latest
  # References the failing-probe template against the real demo-cluster: the integration RESOLVES
  # (sidecar injected, values frozen) but its statusProbe exits non-zero. This exercises the
  # report-only contract -- Degraded on the integration status, Available still True on the workspace.
  integrationTemplateRefs:
    - name: ray-integration-failprobe
      parameters:
        - name: rayClusterName
          value: demo-cluster
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi
