## What

Adds the e2e test suite and its static fixtures (RayCluster CRD + CR, workspace
manifests, and the Ray integration strategy) exercising the full
WorkspaceIntegrationStrategy resolution path against a Kind cluster.

## Scope

e2e test + fixtures only.

## Dependencies

Depends on all the other PRs (API, resolver, apply, watch). **CI will be red
until those merge**; I'll rebase and push a new revision then.

## Testing

- `make test-e2e` (Kind-based; runs the integration strategy suite)
